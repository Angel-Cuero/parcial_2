name: parcial_2
description: "Accidentes Tuluá + CRUD Establecimientos — Parcial 2"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # HTTP client
  dio: ^5.7.0

  # Navigation
  go_router: ^14.6.3

  # Environment variables
  flutter_dotenv: ^5.2.1

  # Charts
  fl_chart: ^0.69.0

  # Skeleton loading
  skeletonizer: ^2.1.3

  # Image picker for logo
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - .env
